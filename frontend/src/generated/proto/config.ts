// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               v3.21.12
// source: proto/config.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "";

/** Represents WiFi connection credentials */
export interface WifiCredentials {
  /** The SSID of the WiFi network to connect to */
  ssid: string;
  /** The password of the WiFi network to connect to */
  pass: string;
}

/** Stores all persistent device configuration loaded at startup */
export interface DeviceConfig {
  /** Optional WiFi credentials. If absent, fallback to defaults. */
  wifi:
    | WifiCredentials
    | undefined;
  /** Global LED brightness level (0-255) */
  brightness: number;
  /** Minimum minutes before a train arrives to show it */
  minMinutes?: number | undefined;
}

/** Request to update the device configuration following AIP-134 */
export interface UpdateConfigRequest {
  /** The configuration to update. */
  config:
    | DeviceConfig
    | undefined;
  /** The list of fields to update. */
  updateMask: string[];
}

function createBaseWifiCredentials(): WifiCredentials {
  return { ssid: "", pass: "" };
}

export const WifiCredentials: MessageFns<WifiCredentials> = {
  encode(message: WifiCredentials, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ssid !== "") {
      writer.uint32(10).string(message.ssid);
    }
    if (message.pass !== "") {
      writer.uint32(18).string(message.pass);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WifiCredentials {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWifiCredentials();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.ssid = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.pass = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WifiCredentials {
    return {
      ssid: isSet(object.ssid) ? globalThis.String(object.ssid) : "",
      pass: isSet(object.pass) ? globalThis.String(object.pass) : "",
    };
  },

  toJSON(message: WifiCredentials): unknown {
    const obj: any = {};
    if (message.ssid !== "") {
      obj.ssid = message.ssid;
    }
    if (message.pass !== "") {
      obj.pass = message.pass;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<WifiCredentials>, I>>(base?: I): WifiCredentials {
    return WifiCredentials.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<WifiCredentials>, I>>(object: I): WifiCredentials {
    const message = createBaseWifiCredentials();
    message.ssid = object.ssid ?? "";
    message.pass = object.pass ?? "";
    return message;
  },
};

function createBaseDeviceConfig(): DeviceConfig {
  return { wifi: undefined, brightness: 0, minMinutes: undefined };
}

export const DeviceConfig: MessageFns<DeviceConfig> = {
  encode(message: DeviceConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.wifi !== undefined) {
      WifiCredentials.encode(message.wifi, writer.uint32(10).fork()).join();
    }
    if (message.brightness !== 0) {
      writer.uint32(16).uint32(message.brightness);
    }
    if (message.minMinutes !== undefined) {
      writer.uint32(24).uint32(message.minMinutes);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeviceConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeviceConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.wifi = WifiCredentials.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.brightness = reader.uint32();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.minMinutes = reader.uint32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeviceConfig {
    return {
      wifi: isSet(object.wifi) ? WifiCredentials.fromJSON(object.wifi) : undefined,
      brightness: isSet(object.brightness) ? globalThis.Number(object.brightness) : 0,
      minMinutes: isSet(object.minMinutes)
        ? globalThis.Number(object.minMinutes)
        : isSet(object.min_minutes)
        ? globalThis.Number(object.min_minutes)
        : undefined,
    };
  },

  toJSON(message: DeviceConfig): unknown {
    const obj: any = {};
    if (message.wifi !== undefined) {
      obj.wifi = WifiCredentials.toJSON(message.wifi);
    }
    if (message.brightness !== 0) {
      obj.brightness = Math.round(message.brightness);
    }
    if (message.minMinutes !== undefined) {
      obj.minMinutes = Math.round(message.minMinutes);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeviceConfig>, I>>(base?: I): DeviceConfig {
    return DeviceConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeviceConfig>, I>>(object: I): DeviceConfig {
    const message = createBaseDeviceConfig();
    message.wifi = (object.wifi !== undefined && object.wifi !== null)
      ? WifiCredentials.fromPartial(object.wifi)
      : undefined;
    message.brightness = object.brightness ?? 0;
    message.minMinutes = object.minMinutes ?? undefined;
    return message;
  },
};

function createBaseUpdateConfigRequest(): UpdateConfigRequest {
  return { config: undefined, updateMask: [] };
}

export const UpdateConfigRequest: MessageFns<UpdateConfigRequest> = {
  encode(message: UpdateConfigRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.config !== undefined) {
      DeviceConfig.encode(message.config, writer.uint32(10).fork()).join();
    }
    for (const v of message.updateMask) {
      writer.uint32(18).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateConfigRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateConfigRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.config = DeviceConfig.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.updateMask.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateConfigRequest {
    return {
      config: isSet(object.config) ? DeviceConfig.fromJSON(object.config) : undefined,
      updateMask: globalThis.Array.isArray(object?.updateMask)
        ? object.updateMask.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.update_mask)
        ? object.update_mask.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: UpdateConfigRequest): unknown {
    const obj: any = {};
    if (message.config !== undefined) {
      obj.config = DeviceConfig.toJSON(message.config);
    }
    if (message.updateMask?.length) {
      obj.updateMask = message.updateMask;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateConfigRequest>, I>>(base?: I): UpdateConfigRequest {
    return UpdateConfigRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateConfigRequest>, I>>(object: I): UpdateConfigRequest {
    const message = createBaseUpdateConfigRequest();
    message.config = (object.config !== undefined && object.config !== null)
      ? DeviceConfig.fromPartial(object.config)
      : undefined;
    message.updateMask = object.updateMask?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
